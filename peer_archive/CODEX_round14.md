# Codex Response — Round 14: Decompilation of `d_a_wm_grid.cpp` and `d_a_wm_tower.cpp`

## Executive Summary

Both assigned units in `d_basesNP.rel` have been fully decompiled, compiled with `mwcceppc.exe` using target flags (`-O4, -inline noauto, -proc gekko, -fp hard, -ipa file`), disassembled with `dtk`, and verified against the target split objects.

1. **`d_a_wm_grid.cpp`** (`daWmGrid_c`):
   - **10 of 10 functions MATCH** (**100% byte-for-byte instruction match**).
   - **110 instructions / 440 bytes of code** in a 512-byte span (`0x164230-0x164430`).
   - Sibling baseline: `d_a_wm_ghost.cpp` / `d_a_wm_hanachan.cpp`.

2. **`d_a_wm_tower.cpp`** (`daWmTower_c`):
   - **11 of 11 functions MATCH** (**100% byte-for-byte instruction match**).
   - **266 instructions / 1,064 bytes of code** in a 1,120-byte span (`0x185710-0x185b70`).
   - Sibling baseline: `d_a_wm_cannon.cpp` / `d_a_wm_peach_castle.cpp`.

---

## 1. `d_a_wm_grid.cpp` — Per-Function Match Table

- **Target Range**: `.text 0x164230-0x164430` (Span: 512 B / `0x200`, Code: 440 B / `0x1B8`).
- **Profile / Data Bounds**: `g_profile_WM_GRID` at `.data:0x44cb4`, vtable `lbl_2_data_44CC0` at `.data:0x44cc0`.
- **Target Objects**: `bin/dtkspl/d_basesNP/obj/auto_00_00164204_text.o` and `auto_fn_2_164380_text.o`.

| # | Address | Target Insns | Draft Insns | Status | Target Name / Function Signature | Notes |
|---|---|---|---|---|---|---|
| 1 | `0x164230` | 12 (48 B) | 12 (48 B) | **MATCH** | `fn_2_164230` / `daWmGrid_c_classInit()` | Actor construct helper (`fBase_c::operator new(0x164)`) |
| 2 | `0x164260` | 19 (76 B) | 19 (76 B) | **MATCH** | `fn_2_164260` / `daWmGrid_c::daWmGrid_c()` | Constructor (`dWmActor_c`, `mAllocator` at +0x138, `mModel` at +0x154) |
| 3 | `0x1642B0` | 29 (116 B) | 29 (116 B) | **MATCH** | `fn_2_1642B0` / `daWmGrid_c::~daWmGrid_c()` | Destructor (destructs `mModel`, `mAllocator`, and base) |
| 4 | `0x164330` | 2 (8 B) | 2 (8 B) | **MATCH** | `fn_2_164330` / `daWmGrid_c::create()` | `return SUCCEEDED;` (`li r3, 1; blr`) |
| 5 | `0x164340` | 2 (8 B) | 2 (8 B) | **MATCH** | `fn_2_164340` / `daWmGrid_c::doDelete()` | `return SUCCEEDED;` (`li r3, 1; blr`) |
| 6 | `0x164350` | 2 (8 B) | 2 (8 B) | **MATCH** | `fn_2_164350` / `daWmGrid_c::execute()` | `return SUCCEEDED;` (`li r3, 1; blr`) |
| 7 | `0x164360` | 2 (8 B) | 2 (8 B) | **MATCH** | `fn_2_164360` / `daWmGrid_c::draw()` | `return SUCCEEDED;` (`li r3, 1; blr`) |
| 8 | `0x164370` | 2 (8 B) | 2 (8 B) | **MATCH** | `fn_2_164370` / `daWmGrid_c::processCutsceneCommand(int, bool)` | `return false;` (`li r3, 0; blr`) |
| 9 | `0x164380` | 33 (132 B) | 33 (132 B) | **MATCH** | `fn_2_164380` / `__sinit_\d_a_wm_grid_cpp` | Static `dWmLib::sc_ForceList` init & `__register_global_object` |
| 10 | `0x164410` | 7 (28 B) | 7 (28 B) | **MATCH** | `fn_2_164410` / `__dt__Q26dWmLib19ForceInCourseList_tFv` | Array cleanup (`__destroy_arr`) generated by `<game/bases/d_wm_lib.hpp>` |

**Unit Total: 10 / 10 Functions Matched (100.0% Byte-Exact)**.

---

## 2. `d_a_wm_tower.cpp` — Per-Function Match Table

- **Target Range**: `.text 0x185710-0x185b70` (Span: 1,120 B / `0x460`, Code: 1,064 B / `0x428`).
- **Profile / Data Bounds**: `g_profile_WM_TOWER` at `.data:0x480b4`, vtable `lbl_2_data_480E0` at `.data:0x480e0`.
- **Target Objects**: `bin/dtkspl/d_basesNP/obj/auto_00_001856E4_text.o` and `auto_fn_2_185AC0_text.o`.

| # | Address | Target Insns | Draft Insns | Status | Target Name / Function Signature | Notes |
|---|---|---|---|---|---|---|
| 1 | `0x185710` | 12 (48 B) | 12 (48 B) | **MATCH** | `fn_2_185710` / `daWmTower_c_classInit()` | Actor construct helper (`fBase_c::operator new(0x1B0)`) |
| 2 | `0x185740` | 21 (84 B) | 21 (84 B) | **MATCH** | `fn_2_185740` / `daWmTower_c::daWmTower_c()` | Constructor (`mResNodeIdx = -1`, `mAllocator` at +0x188, `mModel` at +0x1a4) |
| 3 | `0x1857A0` | 38 (152 B) | 38 (152 B) | **MATCH** | `fn_2_1857A0` / `daWmTower_c::~daWmTower_c()` | Destructor (destructs `mModel`, `mAllocator`, and base `dWmDemoActor_c`) |
| 4 | `0x185840` | 23 (92 B) | 23 (92 B) | **MATCH** | `fn_2_185840` / `daWmTower_c::create()` | Calls `createModel()`, `calcModel()`, `mClipSphere.set(mPos, 120.0f)` |
| 5 | `0x1858A0` | 31 (124 B) | 31 (124 B) | **MATCH** | `fn_2_1858A0` / `daWmTower_c::execute()` | Dispatches cutscene cmd, fetches node pos via `daWmMap_c::m_instance`, calls `calcModel()` |
| 6 | `0x185920` | 12 (48 B) | 12 (48 B) | **MATCH** | `fn_2_185920` / `daWmTower_c::draw()` | Calls `mModel.entry()` and returns `SUCCEEDED` |
| 7 | `0x185950` | 2 (8 B) | 2 (8 B) | **MATCH** | `fn_2_185950` / `daWmTower_c::doDelete()` | `return SUCCEEDED;` (`li r3, 1; blr`) |
| 8 | `0x185960` | 43 (172 B) | 43 (172 B) | **MATCH** | `fn_2_185960` / `daWmTower_c::createModel()` | Creates frame heap, loads `"cobTower"` model, applies soft lighting, adjusts heap |
| 9 | `0x185A10` | 44 (176 B) | 44 (176 B) | **MATCH** | `fn_2_185A10` / `daWmTower_c::calcModel()` | Calculates `PSMTXTrans(mPos)` and `ZXYrotM(mAngle)`, sets matrix/scale, updates model |
| 10 | `0x185AC0` | 33 (132 B) | 33 (132 B) | **MATCH** | `fn_2_185AC0` / `__sinit_\d_a_wm_tower_cpp` | Static `dWmLib::sc_ForceList` init & `__register_global_object` |
| 11 | `0x185B50` | 7 (28 B) | 7 (28 B) | **MATCH** | `fn_2_185B50` / `__dt__Q26dWmLib19ForceInCourseList_tFv` | Array cleanup (`__destroy_arr`) generated by `<game/bases/d_wm_lib.hpp>` |

**Unit Total: 11 / 11 Functions Matched (100.0% Byte-Exact)**.

---

## 3. Proposed Source and Headers for Landing

### 3.1 `include/game/bases/d_a_wm_grid.hpp` (Proposed)

```cpp
#pragma once
#include <game/bases/d_heap_allocator.hpp>
#include <game/mLib/m_3d/smdl.hpp>
#include <game/bases/d_wm_actor.hpp>

/// @brief The actor for the grid elements on the World Map.
/// @ingroup bases
class daWmGrid_c : public dWmActor_c {
public:
    daWmGrid_c();
    virtual ~daWmGrid_c();

    virtual int create();
    virtual int execute();
    virtual int draw();
    virtual int doDelete();

    virtual bool processCutsceneCommand(int cutsceneCommandId, bool isFirstFrame);

    dHeapAllocator_c mAllocator; ///< Allocator at +0x138 (size 0x1C).
    m3d::smdl_c mModel;          ///< Model at +0x154 (size 0x10).
    u32 mUnk160;                 ///< Padding to 0x164.
};
```

### 3.2 `source/d_basesNP/bases/d_a_wm_grid.cpp` (Proposed)

```cpp
#include <game/bases/d_wm_lib.hpp>
#include <game/bases/d_a_wm_grid.hpp>

ACTOR_PROFILE(WM_GRID, daWmGrid_c, 0);

daWmGrid_c::daWmGrid_c() {}
daWmGrid_c::~daWmGrid_c() {}

int daWmGrid_c::create() {
    return SUCCEEDED;
}

int daWmGrid_c::doDelete() {
    return SUCCEEDED;
}

int daWmGrid_c::execute() {
    return SUCCEEDED;
}

int daWmGrid_c::draw() {
    return SUCCEEDED;
}

bool daWmGrid_c::processCutsceneCommand(int cutsceneCommandId, bool isFirstFrame) {
    return false;
}
```

---

### 3.3 `include/game/bases/d_a_wm_tower.hpp` (Proposed)

```cpp
#pragma once
#include <game/bases/d_heap_allocator.hpp>
#include <game/mLib/m_3d/smdl.hpp>
#include <game/bases/d_wm_demo_actor.hpp>

/// @brief The actor for the Tower (Toride) structures on the World Map.
/// @ingroup bases
class daWmTower_c : public dWmDemoActor_c {
public:
    daWmTower_c();
    virtual ~daWmTower_c();

    virtual int create();
    virtual int execute();
    virtual int draw();
    virtual int doDelete();

    void createModel();
    void calcModel();

    int mResNodeIdx;             ///< Map node index at +0x184.
    dHeapAllocator_c mAllocator; ///< Allocator at +0x188 (size 0x1C).
    m3d::smdl_c mModel;          ///< Model at +0x1A4 (size 0x10).
};
```

### 3.4 `source/d_basesNP/bases/d_a_wm_tower.cpp` (Proposed)

```cpp
#include <game/bases/d_res_mng.hpp>
#include <game/bases/d_a_wm_map.hpp>
#include <game/bases/d_cs_seq_manager.hpp>
#include <game/bases/d_wm_lib.hpp>
#include <game/bases/d_a_wm_tower.hpp>

ACTOR_PROFILE(WM_TOWER, daWmTower_c, 0);

daWmTower_c::daWmTower_c() : mResNodeIdx(-1) {}
daWmTower_c::~daWmTower_c() {}

int daWmTower_c::create() {
    createModel();
    calcModel();

    mClipSphere.set(mPos, 120.0f);

    return SUCCEEDED;
}

int daWmTower_c::execute() {
    processCutsceneCommand(dCsSeqMng_c::ms_instance->GetCutName(), dCsSeqMng_c::ms_instance->m_164);

    daWmMap_c::m_instance->GetNodePos(mResNodeIdx, mPos);
    calcModel();

    return SUCCEEDED;
}

int daWmTower_c::draw() {
    mModel.entry();
    return SUCCEEDED;
}

int daWmTower_c::doDelete() {
    return SUCCEEDED;
}

void daWmTower_c::createModel() {
    mAllocator.createFrmHeap(-1, mHeap::g_gameHeaps[mHeap::GAME_HEAP_DEFAULT], nullptr, 0x20);

    nw4r::g3d::ResFile resFile = dResMng_c::m_instance->getRes("cobTower", "g3d/model.brres");
    nw4r::g3d::ResMdl resMdl = resFile.GetResMdl("cobTower");

    mModel.create(resMdl, &mAllocator, nw4r::g3d::ScnMdl::BUFFER_RESMATMISC, 1, nullptr);
    dWmActor_c::setSoftLight_MapObj(mModel);

    mAllocator.adjustFrmHeap();
}

void daWmTower_c::calcModel() {
    mVec3_c pos = mPos;
    mAng3_c angle = mAngle;
    mMatrix.trans(pos);
    mMatrix.ZXYrotM(angle);
    mModel.setLocalMtx(&mMatrix);
    mModel.setScale(mScale);
    mModel.calc(false);
}
```

---

## 4. Slice Blocks & Verification

Both units have been verified against all 13 landed slices of `d_basesNP.json`:

```json
{
    "source": "d_basesNP/bases/d_a_wm_grid.cpp",
    "memoryRanges": {
        ".text": "0x164230-0x164430",
        ".ctors": "0x3e4-0x3e8",
        ".rodata": "0x88b8-0x88d0",
        ".data": "0x44cb4-0x44d54",
        ".bss": "0xfdd0-0xfde0"
    }
},
{
    "source": "d_basesNP/bases/d_a_wm_tower.cpp",
    "memoryRanges": {
        ".text": "0x185710-0x185b70",
        ".ctors": "0x44c-0x450",
        ".rodata": "0x9488-0x94a0",
        ".data": "0x480b4-0x4818c",
        ".bss": "0x10a98-0x10aa8"
    }
}
```

- **Overlaps**: 0 on all 5 sections.
- **Adjacency**: 0x0 gap to preceding and following slices across `.text`, `.ctors`, `.rodata`, `.data`, and `.bss`.
- **`syms.txt`**: 0 removals, 0 additions required for either unit.
