# Work Queue — wiimj2d vetted decompilation units

Generated by vetting `find_targets` candidates (module=wiimj2d, 200-900 B,
require_named=true) one class at a time against `search_symbols`. A candidate
only made this list if **every** symbol belonging to its class falls inside
one uninterrupted address run matching the stated range, with no foreign
symbols inside and no leftover symbols of the same class sitting elsewhere.
Most candidates from `find_targets` failed this check — see REJECTED at the
bottom before re-evaluating anything.

**Status** values: `UNCLAIMED` / `IN PROGRESS` / `DONE`. An agent must edit
the `Status:` line of a unit to `IN PROGRESS: <agent>` before starting work
on it, and to `DONE` (with a note) once it lands. Do not start a unit whose
Status is not UNCLAIMED.

Ordered easiest first.

> **Before adding a unit to this queue, check it is not already decompiled.**
> `slices/wiimj2d.json` lists every landed unit, and `source/dol/bases/` holds
> the landed sources. `dPanelObjMgr_c` was queued here in error and removed —
> it landed on 2026-08-23. `find_targets` does not know what has been done.

> **The stall rule is enforced by the `Attempts:` line, not by memory.**
> Each session starts fresh and cannot recall how many tries previous sessions
> made. So BEFORE you start work on a unit, increment its `Attempts:` count and
> commit that. When it reaches **3**, park the unit, write the state into
> `READY_TO_LAND.md`, set `Status: PARKED`, and claim the next one.
> On 2026-08-23 one function consumed most of a ten-hour run because this
> counter did not exist and every fresh session started from zero.

---

## 1. dLiftAllhitDraw2_c — 200 B, 1 function — EASY
- Range: 0x800BFDD0-0x800BFE98
- Status: DONE (landed 2026-08-24, container-worker). True scope was draw +
- Attempts: 0
  __sinit at 0x800BFEA0 (.bss float[4] at 0x803590F0, .ctors slot 0x802EDDF4);
  landed as dol/bases/d_lift_allhit_draw2.cpp with slice .text 0xB9650-0xB973C.
  The unnamed helpers fn_800BE6E0..fn_800BFCB0 before draw are a closed call
  community that may be the same TU -- left unclaimed, see AUTO_LOG/AGENT_CONTEXT.
- Existing header: none
- Virtual: no evidence (no `__dt`/`__vt__` symbol found)
- Functions (address order):
      200 B  draw__18dLiftAllhitDraw2_cFv   0x800BFDD0
- Vetting: search_symbols("dLiftAllhitDraw2_c") returns exactly this 1 symbol,
  matching the find_targets candidate exactly. No other class symbols nearby.

## 2. dRandom_c — 248 B, 1 function — EASY
- Range: 0x800D9850-0x800D9948
- Status: PARKED (container-worker, 2026-08-24). See READY_TO_LAND.md PARKED
- Attempts: 0
  section: code structure fully decoded, best draft 63w/54 diffs vs 62w target,
  blocked on a compiler addressing-mode difference no source shape reached in
  ~27 sweeps across two sessions.
- Existing header: none
- Virtual: no evidence
- Functions (address order):
      248 B  calcMachineRandom__9dRandom_cFv   0x800D9850
- Vetting: search_symbols("dRandom_c") returns exactly this 1 symbol. Class
  is a trivial single-function utility here (other dRandom_c usage, if any,
  is presumably already decompiled elsewhere and did not surface).

## 3. dFunsuiAct_c — 332 B, 1 function — EASY
- Range: 0x800B2D20-0x800B2E6C
- Status: DONE (landed 2026-08-24, container-worker). posMove 83/83 words,
  byte-verified; landed as dol/bases/d_funsui_act.cpp with slices
  .text 0xac5a0-0xac6ec, .data 0x17228-0x17238 (Wm_en_quicksand string),
  .sdata2 0x14e0-0x14f0 (function-local cs_rev_speed[2] + two pool floats).
  New header include/game/bases/d_funsui_act.hpp.
- Attempts: 2 (prior session's ~40 sweeps = attempt 1; closed on attempt 2)
- Existing header: none
- Virtual: no evidence
- Functions (address order):
      332 B  posMove__12dFunsuiAct_cFv   0x800B2D20
- Vetting: search_symbols("dFunsuiAct_c") returns exactly this 1 symbol.

## 4. LangLocString — 404 B, 1 function — EASY
- Range: 0x80107BE0-0x80107D74
- Status: DONE (landed 2026-08-24, container-worker). Ctor 101/101 words,
  byte-verified on first compile; landed as dol/bases/d_lang_loc_string.cpp
  with slices .text 0x101460-0x1015f4 and .sdata 0x1a40-0x1b64 (the TU owns
  all 45 locale-code string literals, 0x804293C0-0x804294E4). New header
  include/game/bases/d_lang_loc_string.hpp; no external symbols.
- Attempts: 1
- Existing header: none
- Virtual: no evidence
- Functions (address order):
      404 B  __ct__13LangLocStringFv   0x80107BE0
- Vetting: search_symbols("LangLocString") returns exactly this 1 symbol
  (a single, fairly large constructor — likely a string-table/locale setup
  routine with inline data, worth a quick check of what it constructs).

## 5. dRomFontMgr_c — 224 B, 2 functions — EASY
- Range: 0x80106F00-0x80106FE4
- Status: UNCLAIMED
- Attempts: 0
- Existing header: include/game/bases/d_rom_font_manager.hpp
- Virtual: no evidence
- Functions (address order):
      108 B  createInstance__13dRomFontMgr_cFPQ23EGG4Heap   0x80106F00
      116 B  load_resource__13dRomFontMgr_cFPQ23EGG4Heap    0x80106F70
- Vetting: search_symbols("dRomFontMgr_c") returns exactly these 2 symbols,
  contiguous, matching the candidate. Header already exists, so this is
  purely filling in two method bodies.

## 6. dDvdErrorWideMsg_c — 268 B, 2 functions — EASY
- Range: 0x801079A0-0x80107AB8
- Status: UNCLAIMED
- Attempts: 0
- Existing header: none
- Virtual: no evidence
- Functions (address order):
      132 B  __ct__18dDvdErrorWideMsg_cFv          0x801079A0
      136 B  initialize__18dDvdErrorWideMsg_cFPPCc  0x80107A30
- Vetting: search_symbols("dDvdErrorWideMsg_c") returns exactly these 2
  symbols, contiguous, matching the candidate.

## 7. dRotShake_c — 292 B, 2 functions — EASY
- **Head start:** Partial work exists at `scratch/trial/` — init already at DIFFS 0. `move` is 63/63 words, same register-mirror character. Start from that source, do not restart.
- Range: 0x800DF950-0x800DFA7C
- Status: UNCLAIMED
- Attempts: 0
- Existing header: none
- Virtual: no evidence
- Functions (address order):
       40 B  init__11dRotShake_cFssssssss   0x800DF950
      252 B  move__11dRotShake_cFv          0x800DF980
- Vetting: search_symbols("dRotShake_c") returns exactly these 2 symbols,
  contiguous, matching the candidate.

## 8. dScRestartCrsin_c — 360 B, 2 functions — EASY
- Range: 0x801018E0-0x80101A48
- Status: UNCLAIMED
- Attempts: 0
- Existing header: include/game/bases/d_s_restart_crsin.hpp
- Virtual: no evidence
- Functions (address order):
      232 B  startTitle__17dScRestartCrsin_cFUcb        0x801018E0
      120 B  reStartPeachCastle__17dScRestartCrsin_cFv  0x801019D0
- Vetting: search_symbols("dScRestartCrsin_c") returns exactly these 2
  symbols, contiguous, matching the candidate. Header already exists.

## 9. dPosShake_c — 288 B, 3 functions — EASY
- **Head start:** Partial work exists at `scratch/trial/` — 2 of 3 already at DIFFS 0 (init, startShake). `move` is 61/61 words with a single f1/f2 register mirror. Start from that source, do not restart.
- Range: 0x800D81A0-0x800D82D0
- Status: UNCLAIMED
- Attempts: 0
- Existing header: none
- Virtual: no evidence
- Functions (address order):
       28 B  init__11dPosShake_cFffffff    0x800D81A0
      244 B  move__11dPosShake_cFv         0x800D81C0
       16 B  startShake__11dPosShake_cFf   0x800D82C0
- Vetting: search_symbols("dPosShake_c") returns exactly these 3 symbols,
  contiguous, matching the candidate. (Sibling class dRotShake_c, unit #7
  above, is the rotational counterpart — same pattern, likely same source
  directory; not the same TU since addresses are far apart.)

## 10. MsgRes_c — 220 B, 4 functions — EASY/MEDIUM
- Range: 0x800CE7F0-0x800CE8E4
- Status: UNCLAIMED
- Attempts: 0
- Existing header: include/game/bases/d_message.hpp (also referenced from
  d_lyttextBox.hpp and d_tag_processor.hpp, which take `MsgRes_c*` params —
  those are unrelated classes, not part of this unit)
- Virtual: YES — `__vt__8MsgRes_c` at .data:0x80317CB8, size 0xC (base info +
  1 virtual function pointer)
- Functions (address order):
       60 B  __ct__8MsgRes_cFPCvPQ23EGG4Heap   0x800CE7F0
       88 B  __dt__8MsgRes_cFv                 0x800CE830
       36 B  getScale__8MsgRes_cFUlUl          0x800CE890
       36 B  getFont__8MsgRes_cFUlUl           0x800CE8C0
- Vetting: search_symbols("MsgRes_c") returns many hits, but only these 4
  are actually `MsgRes_c`'s own symbols — the rest (LytTextBox_c::setMessage,
  TagProcessor_c::*) merely take `MsgRes_c*`/`MsgRes_c&` as a parameter and
  are already decompiled (`[done: ...]`). These 4 are contiguous and match
  the candidate exactly. Header already exists.

## 12. dPyEffectMng_c — 408 B, 4 functions — EASY/MEDIUM
- Range: 0x800D2D10-0x800D2EBC
- Status: UNCLAIMED
- Attempts: 0
- Existing header: include/game/bases/d_player_effect_manager.hpp (also
  referenced from d_a_player_manager.hpp)
- Virtual: YES — `__vt__14dPyEffectMng_c` at .data:0x80317E08, size 0xC
  (base info + 1 virtual function pointer)
- Functions (address order):
       92 B  __ct__14dPyEffectMng_cFv                                0x800D2D10
      104 B  __dt__14dPyEffectMng_cFv                                0x800D2D70
      136 B  fn_800d2de0__14dPyEffectMng_cFfiR7mVec3_cUc (unnamed)   0x800D2DE0
       76 B  update__14dPyEffectMng_cFv                              0x800D2E70
- Vetting: search_symbols("dPyEffectMng_c") returns exactly these 4 symbols,
  contiguous, matching the candidate. Header exists; only 1 virtual slot
  keeps this near-trivial despite the vtable. Note the third function has
  no demangled name yet (`fn_800d2de0`) — figure out its real name/signature
  from context (likely an internal effect-spawn helper) while implementing.

## 13. daZoomPipeBase_c — 388 B, 2 functions — MEDIUM
- Range: 0x80063F80-0x80064104
- Status: UNCLAIMED
- Attempts: 0
- Existing header: none
- Virtual: unclear — no `__dt`/`__vt__` symbol surfaced for this class in
  this run; the `Base_c` suffix strongly implies it is inherited from
  elsewhere and may only add non-virtual methods here, or its vtable/dtor
  are already decompiled. Confirm before assuming a plain struct.
- Functions (address order):
      176 B  init__16daZoomPipeBase_cFUi   0x80063F80
      212 B  execute__16daZoomPipeBase_cFv 0x80064030
- Vetting: search_symbols("daZoomPipeBase_c") returns exactly these 2
  symbols, contiguous, matching the candidate. Marked MEDIUM because base
  classes often need layout inferred from a derived class; no header exists
  yet to shortcut that.

## 14. dDrawShadowModel_c — 312 B, 4 functions — MEDIUM
- **WARNING — vetting passed this but I have evidence against it.** An
  independent analysis found `dDrawShadowModel_c` shares ONE retail translation
  unit (0x8008A004-0x8008C200, ~8.7 KB) with six sibling classes: `dCapture_c`,
  `dSetupGX_c`, `dCaptureCoin_c`, `dDrawScreen_c`, `dMakeShadowTex_c`,
  `dDrawShadowProjMap_c`. The tell is that every class's weak inline
  `drawXlu`/`__dt` bodies are bunched at the END of the object in reverse
  declaration order, and their vtables run contiguously in `.data` from
  0x80310FB0 to 0x803110A0. It also needs the base class `d3d::proc_c`, which
  has no header (see AGENT_CONTEXT "An empty base class needs a declaration and
  a syms.txt pin"). Carving one class out by byte range may be possible but the
  file-local helpers `fn_8008B830` / `fn_8008BA00` fall outside the carve.
  **Treat as the whole ~8.7 KB group or skip.**
- Range: 0x8008BE60-0x8008BFB0
- Status: UNCLAIMED
- Attempts: 0
- Existing header: none
- Virtual: YES — `__vt__18dDrawShadowModel_c` at .data:0x80310FB0, size 0x20
  (base info + 6 virtual function pointers)
- Functions (address order):
      112 B  create__18dDrawShadowModel_cFP12mAllocator_ci   0x8008BE60
      100 B  drawOpa__18dDrawShadowModel_cFv                 0x8008BED0
        4 B  drawXlu__18dDrawShadowModel_cFv                 0x8008BF40
       96 B  __dt__18dDrawShadowModel_cFv                    0x8008BF50
- Vetting: search_symbols("dDrawShadowModel_c") returns exactly these 4
  symbols, contiguous, matching the candidate. 6-entry vtable (some entries
  likely inherited from a shared "draw proc" base, cf. dSetupGX_c-style
  classes seen elsewhere in this module) means layout must be inferred —
  no header exists.

## 15. KokoopaSpFumiCheck_c — 408 B, 2 functions — MEDIUM
- Range: 0x800B0840-0x800B09E0
- Status: UNCLAIMED
- Attempts: 0
- Existing header: none
- Virtual: YES — `__vt__20KokoopaSpFumiCheck_c` at .data:0x80315298, size
  0x10 (base info + 2 virtual function pointers)
- Functions (address order):
      344 B  operate__20KokoopaSpFumiCheck_cFRiP5dEn_cR12FumiCcInfo_c 0x800B0840
       64 B  __dt__20KokoopaSpFumiCheck_cFv                            0x800B09A0
- Vetting: search_symbols("KokoopaSpFumiCheck_c") returns exactly these 2
  symbols, contiguous, matching the candidate. Takes `FumiCcInfo_c&` and
  `dEn_c*` — likely a small "fumi check" callback object with a 2-entry
  vtable (probably inherits a `*FumiCheck_c` interface used by other bosses,
  e.g. the interleaved dHanaBody/Head/BigHead group nearby uses a similar
  pattern — check for a common base before writing the header).

## 16. dScEffectProc_c — 292 B, 5 functions — MEDIUM/HARD
- **WARNING — deeper than it looks.** `dScEffectProc_c` derives from
  `dEffectProcBase_c` -> `d3d::proc_c` -> `m3d::scnLeaf_c`. Neither
  `dEffectProcBase_c` nor `d3d::proc_c` exists in `include/`. `drawEffect` also
  needs an `EffectManager_c::draw(int,int)` overload that is not declared.
  Good news: its other dependency, `GXStateSave_c`, LANDED on 2026-08-23 and is
  now at `include/game/bases/d_gx_state_save.hpp`. See AGENT_CONTEXT for the
  d3d::proc_c header shape and its syms pin.
- Range: 0x80092D50-0x80092E94
- Status: UNCLAIMED
- Attempts: 0
- Existing header: none
- Virtual: YES — `__vt__15dScEffectProc_c` at .data:0x80311908, size 0x24
  (base info + 7 virtual function pointers)
- Functions (address order):
       68 B  create__15dScEffectProc_cFP12mAllocator_ciii  0x80092D50
        8 B  drawOpa__15dScEffectProc_cFv                  0x80092DA0
        8 B  drawXlu__15dScEffectProc_cFv                  0x80092DB0
      108 B  drawEffect__15dScEffectProc_cFi                0x80092DC0
      100 B  __dt__15dScEffectProc_cFv                       0x80092E30
- Vetting: search_symbols("dScEffectProc_c") returns exactly these 5
  symbols, contiguous, matching the candidate. 7-entry vtable and the
  create/drawOpa/drawXlu/__dt shape closely mirrors dProcShareProc_c /
  dScnGroupShareProc_c / dSetupGX_c seen during vetting (all REJECTED below
  because their destructors sit far outside their candidate range) — this
  one's destructor IS in range, which is what makes it landable, but expect
  the same "shared scene proc" base class inheritance to require care.

## 17. dLevelEffect_c (dEf::dLevelEffect_c) — 256 B, 2 functions — HARD
- Range: 0x8008FD20-0x8008FE28
- Status: UNCLAIMED
- Attempts: 0
- Existing header: include/game/bases/d_effect.hpp — class already declared:
  `class dLevelEffect_c : public mEf::levelEffect_c { ... virtual ~dLevelEffect_c() {} ... }`
  (also referenced from d_a_en_togezo_base.hpp, d_a_player.hpp)
- Virtual: YES — `__vt__Q23dEf14dLevelEffect_c` at .data:0x803111D8, size
  0xBC (base info + ~45 virtual function pointers, mostly inherited from
  `mEf::levelEffect_c`)
- Functions (address order):
      136 B  isActive__Q23dEf14dLevelEffect_cFv   0x8008FD20
      120 B  __dt__Q23dEf14dLevelEffect_cFv        0x8008FDB0
- Vetting: search_symbols("dLevelEffect_c") returns exactly these 2 symbols,
  contiguous, matching the candidate. HARD despite being only 2 small
  functions because of the large inherited vtable (~45 entries) — get the
  `mEf::levelEffect_c` base layout right first, the header already gives the
  class shape so this is mostly about correctly matching the destructor's
  interaction with the base class chain.

---

## REJECTED (do not attempt)

- **dHanaBody_c** — interleaved in address space with dHanaHead_c and
  dHanaBigHead_c (all three classes' methods are woven together across
  0x800B6C70-0x800B8044); none of the three can land as a standalone TU.
- **dHanaHead_c** — same interleaving as dHanaBody_c above.
- **dHanaBigHead_c** — same interleaving as dHanaBody_c above.
- **mDvd_mountMemArchive_c** — 6 real symbols vs. 2 listed; create/execute/
  getArcBinary/doClear sit outside the candidate's stated 212 B range.
- **dScnGroupShareProc_c** — destructor sits ~730 B away from the 4-function
  body the candidate listed.
- **dProcShareProc_c** — destructor sits ~1.3 KB away from the 4-function
  body the candidate listed.
- **dSetupGX_c** — destructor sits ~6.8 KB away from the 3-function body the
  candidate listed.
- **dWmSeManager_c** — actually a 12-function, ~6.3 KB class; the candidate
  was just 2 leftover functions (endSound/getEmptyHoldSeSlot).
- **dWarningManager_c** — 40+ function warning-UI state-machine class; the
  candidate was 2 leftover functions.
- **dGameDisplay_c** — 40+ function state-machine class; the candidate was
  a single constructor.
- **mDvd_command_c** — interleaved with mDvd_param_c; __ct/destroy sit past
  mDvd_param_c's addCommand/removeCommand, outside the candidate's range.
- **daWmMap_c** — actually a 12-function, ~2 KB class; the candidate was 2
  functions plucked from the middle of it.
- **dWaterEntryMng_c** — actually a 4-function, ~992 B class; the candidate
  undersold it as a single 256 B function.
- **dKinokoKasaDraw_c** — has a third function (`draw`, 664 B) outside the
  2-function candidate range.
- **dLineMng_c** — 40+ function physics/line-manager state-machine class;
  two separate find_targets groups (264 B and 280 B) are both fragments of
  the same giant class.
- **dSaveMng_c** — actually an 18-function, ~6.7 KB class; the candidate was
  2 functions.
- **quad_c** — has 3 more functions (getType/calc/__dt) sitting ~7.5 KB away
  from the candidate's 4-function body.
- **dEnJrClownBase_c** — 39-function enemy base class.
- **dPoleRope_c** — actually a 3-function, ~1092 B contiguous class; the
  candidate undersold it as a single 252 B function.
- **dEnBoss_c** — 40+ function boss base class.
- **daWmPlayer_c** — actually a 15-function class spanning ~1.8 KB; the
  candidate was 3 functions.
- **dMarioMdl_c** — actually a 13-function class; the candidate was 3
  functions.
- **NMSndObjectBase** — actually a 5-function virtual class (incl. `vf1C`);
  the candidate was a single function.
- **dWarningBattery_c** (and its `sFStateID_c<17dWarningBattery_c>`
  template) — 40-function warning state-machine class.
- **dWarningErrorInfo_c** — 40+ function state-machine class.
- **dWarningNunchuk_c** — 40-function state-machine class.
- **dWarningOther_c** — 40-function state-machine class.
- **dWarningYoKo_c** — 40-function state-machine class.
- **dfukidashiInfo_c** — 40-function state-machine class.
- **dIggyWanKusari_c** — 40-function state-machine class.
- **dPosGoAndComeExeFrm_c** — 29-function state-machine class.
- **dReplayPlay_c** — 40-function state-machine class.
- **dWanKusari_c** — 37-function state-machine class.
- **dGoalManager_c** — 40+ function state-machine class.
- **dGameCom** — looked like a 340 B unit, actually a ~7 KB namespace
  (previously identified, see project history).
- **dSwingLightMask_c** — destructor sits 0x370 away from its body
  (previously identified, see project history).
- **dEnFumiCheck_c** — destructor sits ~55 KB away (previously identified,
  see project history).
- **dNandThread_c** — actually an 18-function, ~4 KB class; the candidate
  was a single constructor.
- **dWmMapModel_c** — actually a 38-function, ~6 KB class; the candidate
  was a single destructor.
- **dRemoconMng_c** — actually a 40+ function nested state-machine class
  (with dConnect_c/dExtension_c sub-states woven in); the candidate was 2
  functions.
- **dWmRotater_c** — 2 extra functions (setAmplitude/setOffset) exist after
  a suspicious 132 B gap right past the candidate's stated range.
- **dBgGm_c** — 12-function class including one 6328 B function; the
  candidate was 2 leftover functions.
- **dWipeFader_c** — actually a 4-function, ~904 B contiguous class; the
  candidate undersold it as 2 functions (missing __ct/createTexture).
- **dWmBgmSync_c** — destructor sits ~84 KB away from execute/getAnmRate —
  clearly a different translation unit.
- **daFarBG_HIO_c** — destructor sits ~18 KB away from the constructor.
- **dScWMap_c** — actually an 11-function class (plus 1 already-done)
  spanning ~1.5 KB; the candidate was a single function.
- **dCaveMask_c** — destructor sits ~1.7 KB away from the 4-function body
  the candidate listed.
- **dActorGroupIdMng_c** — has an extra function (`allDoDelete`) sitting
  just before the candidate's stated range.
- **dActorMng_c** — actually a 21-function, ~3.1 KB class; the candidate
  was 5 functions.
- **dSwitchFlagMng_c** — actually an 11-function, ~2.8 KB class
  (find_targets itself flagged "1 more" on this group); the candidate was
  7 functions.
