# WM_ITEM -- daWmItem_c

`.text 0x167120-0x16793c`, 12 functions + one `.ctors`-registered function
(`fn_2_1678F0`), module `d_basesNP`. Base class `dBaseActor_c` (direct,
confirmed from both the ctor's `bl __ct__12dBaseActor_cFv` and the dtor's
`bl __dt__12dBaseActor_cFv`).

## Result: 8/12 byte-identical (modulo isolated-compile symbol names)

| target | size | draft | note |
|---|---|---|---|
| `fn_2_167120` classInit | 0x30 | **MATCH** | generated by `ACTOR_PROFILE(WM_ITEM, daWmItem_c, 0)` |
| `fn_2_167150` ctor | 0xc0 | **MATCH** | |
| `fn_2_167210` dtor | 0x90 | **MATCH** | |
| `fn_2_1672A0` create | 0xa4 | **MATCH** | |
| `fn_2_167350` step0 | 0x64 | **MATCH** | |
| `fn_2_1673C0` step1 | 0x6c | **MATCH** | |
| `fn_2_167430` step2 | 0x6c | **MATCH** | |
| `fn_2_1674A0` doDelete | 0x8 | **MATCH** | trivial `return 1;` |
| `fn_2_1674B0` createModel | 0x298 | 116 differing (166 instr) | largest function; register-allocation shape not yet matched, see Negatives |
| `fn_2_167750` cycleAnm | 0x80 | 14 differing (32 vs 33 instr) | one extra instruction; symbol-name noise removed, see Negatives |
| `fn_2_1677D0` calcModel | 0x118 | 60 differing (70 vs 67 instr) | same register-caching issue as createModel |
| `fn_2_1678F0` `.ctors` init | 0x4c | 12 differing, ALL symbol-name text | structurally exact -- see Negatives |

Every one of the 4 non-matching functions is semantically correct (verified
by direct `difftool.py` diffs, not `verify_anon.py`'s pairing, which
mis-pairs several of these against unrelated weak vtable-thunk destructors
because their instruction counts are close). The residual differences are
register-allocation/addressing-mode shape, not logic bugs.

## Class layout -- measured and compiler-verified

Confirmed by a `STATIC_ASSERT`/`Probe<N>`-style compile (`wip/wm_units/agent_item/probe.cpp`)
against the REAL headers already in `include/`, cross-checked instruction-by-
instruction against the ctor's explicit field-zeroing stores:

```cpp
class daWmItem_c : public dBaseActor_c {   // sizeof == 0x210, matches `li r3,0x210`
    dHeapAllocator_c mAllocator;            // @0x128
    m3d::mdl_c mModel;                      // @0x144
    m3d::anmChr_c mAnmChr;                  // @0x184, measured sizeof 0x38
    nw4r::g3d::ResFile mResFile;            // @0x1bc, main archive
    nw4r::g3d::ResFile mAnimResFile;        // @0x1c0, animation archive
    nw4r::g3d::ResAnmTexPat mResAnmTexPat;  // @0x1c4, transient, createModel() only
    m3d::anmTexPat_c mAnmTexPat;            // @0x1c8, measured sizeof 0x2c
    float mMotion[3];                       // @0x1f4
    int m_200, m_204;                       // @0x200/0x204 -- signed (target uses cmpw not cmplw)
    u8 m_208, m_209, m_20a, m_20b;          // @0x208-0x20b, state flags
    int mItemType;                          // @0x20c, mParam & 0xF -- signed (cmpwi not cmplwi)
};
```

This is the strongest-grounded layout of any unit this session: every
offset was independently confirmed two ways -- the `Probe<N>` compile error
text (`illegal use of incomplete struct/union/class 'Probe<444>'` etc, read
as decimal) AND the raw store addresses in `fn_2_167150`'s disassembly.
`mAnmTexPat`'s embedded `banm_c::mAllocator` sub-object ctor call
(`bl __ct__12mAllocator_cFv`, `r3 = this+0x1d4`) independently confirms
`mAnmTexPat` starts at `0x1c8` and that `EGG::Allocator` (via `mAllocator_c`)
is 0x1c bytes, not the 4 a naive "just a vtable pointer" guess would give --
this is what makes `sizeof(anmTexPat_c) == 0x2c`, not 0x14.

`this+0x60` is the usual auto-populated "secondary vtable" pointer (see prior
units' notes) -- no manual field or code needed for it.

## Data layout -- measured from real relocations, not guessed

`g_profile_WM_ITEM` (`.data 0x45030`, size 0xc, standard `fActorProfile_c`)
has its `classInit` field's relocation resolving directly to `fn_2_167120`
(`.text 0x167120`) -- proven with `tools/relfile.py` against
`original/d_basesNP.rel`'s own self-relocation stream (module index 2), not
inferred. This is why `.data` is claimed starting at the profile despite
`check_bounds.py`'s "wm unit opens on sc_ForceList" heuristic flagging it --
that heuristic is a *default*, not a universal rule, and here it is directly
overridden by the classInit self-relocation. `d_a_wm_kinoko_base.cpp`
(landed sibling, `WM_KINOKO_BASE`) and `WM_ISLAND` (immediately preceding
unit in `.data`, ending exactly at `0x45030` with no gap) were both read for
precedent.

Everything from `.data 0x45030` through `0x45230` (`lbl_2_data_451B8`, the
secondary vtable, ending exactly there) belongs to this unit -- confirmed by
scanning every `.text`-sourced relocation in `[0x167120, 0x16793c)` and
finding none past `0x451b8`. `.data 0x45230-0x45270` (two small objects plus
two 5-byte strings, `WM_KILLER`'s own `sc_ForceList`-shaped opener) is
**not** referenced by this unit and is excluded from the claim.

The tables following the profile (all pointer arrays of 7 entries, one per
item type 0-6, index 5 always repeating index 0's entry -- 5 real distinct
item types plus one alias):

| profile offset | address | content | used by |
|---|---|---|---|
| +0x68 | 0x45098 | `I_kinoko`/`I_fireflower`/`I_propeller`/`I_iceflower`/`I_penguin`/`I_kinoko`/`I_star` | archive name (arcName) + resPath component |
| +0x98 | 0x450c8 | same, except index 2 = `I_propeller_model` | `GetResMdl`'s model name |
| +0x108 | 0x45138 | `SI_kinoko`/`SI_fireflower`/... | animation-archive arcName |
| +0x124 | 0x45154 | `"g3d/"` (constant) | resPath prefix |
| +0x12c | 0x4515c | `".brres"` (constant) | resPath suffix |
| +0x138 | 0x45168 | `"g3d/model.brres"` (constant, full path) | animation archive's resPath |
| +0x148 | 0x45178 | `"stok_wait"` (constant) | initial `mAnmChr` anim name |
| +0x158 | 0x45188 | `"I_kinoko_switch"` (constant) | `mAnmTexPat`'s anim name (item type 0/5 only) |

`lbl_2_data_451B0` (0x8 bytes, 2 pointers -- profile-independent, own
symbol) = `{"stok_wait" (0x45198), "stok_wait2" (0x451a4)}`, indexed by
`m_204` in `cycleAnm()`.

`lbl_2_rodata_8988` (0x18, 6 floats) and `lbl_2_rodata_89A0` (0x18, 6 more)
are two adjacent `dtk`-split symbols that the target's own code treats as
ONE 12-float table -- `create()`'s item-type-5 branch reads
`lbl_2_rodata_8988+0x1c`, past the first object's declared 0x18-byte size,
through the SAME base register used for the first 6. Values (read directly
from `original/d_basesNP.rel` .rodata, file offset `0x1c6600+addr`):
`{-32, 200, 3.2, 0.65, 0, 1000, 1.0, 0.65, 2.0, 3.2, -32, 200}`.

`lbl_2_data_4503C` (0x14 bytes) is patched at load by the guarded `.ctors`
function (`fn_2_1678F0`, castle/dance_pakkun-shaped: `s8` guard at
`lbl_2_bss_FDF8`, `extsb.`/`bnelr` test) with the first 4 floats of the
table above. The trailing 4 bytes (offset 0x10) are never referenced by any
of this unit's 12 functions.

## Proposed slice

```json
{
  "source": "d_basesNP/bases/d_a_wm_item.cpp",
  "memoryRanges": {
    ".text": "0x167120-0x16793c",
    ".data": "0x45030-0x45230",
    ".rodata": "0x8988-0x89b8",
    ".bss": "0xfdf8-0xfe00",
    ".ctors": "0x3ec-0x3f0"
  }
}
```

`check_bounds.py d_basesNP` on this claim: `.text`/`.rodata`/`.bss`/`.ctors`
report clean (the `.ctors` "no symbols in range" note is expected -- dtk
doesn't label `.ctors` entries). `.data`'s only flagged item is the
profile-opens-the-unit heuristic, overridden by the classInit
self-relocation evidence above -- not a real defect.

## Negatives (every one measured, not guessed)

1. **`create()`/`step0`/`step1`/`step2` are never called from any of this
   unit's other 11 functions.** They must be reached through some
   polymorphic mechanism outside this unit's own code (likely the
   "secondary vtable" at `this+0x60`, auto-populated by the compiler for
   some base interface this class implements alongside `dBaseActor_c`).
   `create()`'s exact signature is itself uncertain: the target never sets
   `r3` before returning (no `li r3,SUCCEEDED`), which is why `virtual int
   create()` returning `SUCCEEDED` is a guess, not a measurement -- `mwcc`
   rejects a `void create()` override of `fBase_c::create()` (error 10227),
   so *some* int-returning shape is required, but the real return value is
   unconfirmed.

2. **`createModel()`, `calcModel()`, and `cycleAnm()` all show the same
   residual defect class**: the target caches `lbl_2_rodata_8988`'s (or
   `lbl_2_rodata_89A0`'s) base address in one dedicated register for the
   whole function (e.g. `calcModel()`'s `r31`), while the isolated draft
   recomputes the constant-table address at each access site, costing one
   extra `lis`/`addi` pair per site and cascading a register-allocation
   shape mismatch through the rest of the function. Introducing a local
   `const float *k = sConstTable;` alias measurably helped `calcModel()`
   (68 -> 60 differing) but did not fully resolve it. This is the same open
   problem `dance_pakkun`'s `createModel()` hit with its own play-mode
   table and was never fully solved there either -- it looks like a
   systemic pattern for this project's isolated-compile register allocator,
   not a one-off bug in this unit.

3. **`fn_2_167750` (`cycleAnm`) is otherwise exact**: every one of its 17
   `difftool.py`-reported differences is either (a) the same rodata-caching
   issue as above (one extra instruction, cascading the rest), or (b) a
   symbol-name text difference (`lbl_2_data_451B0` vs the local
   `sCycleAnmNames`) that will resolve once linked against the real
   addresses. No logic differs.

4. **`fn_2_1678F0` (`.ctors` init) is fully structurally exact.** Every one
   of the 12 `difftool.py`-reported differences is symbol-name text
   (`lbl_2_bss_FDF8`/`lbl_2_rodata_8988`/`lbl_2_data_4503C` vs this
   isolated compile's own anonymous-namespace object names) or an offset
   difference caused by the merged 12-float `sConstTable` sitting at a
   different position relative to *this function's own* anonymous pool than
   the real target's `lbl_2_rodata_8988` sits relative to *its* neighbours
   -- not a content or ordering defect.

5. **Fixed two real bugs found only by measuring, not by design intuition**:
   `mItemType`/`m_200`/`m_204` must be `int`, not `u32` -- the target
   compares them with `cmpwi`/`cmpw` (signed), and using unsigned types
   produced `cmplwi`/`cmplw` instead, a real byte-level defect on `create()`
   and (partially) `cycleAnm()`. And chained assignment (`mScale.x =
   mScale.y = mScale.z = v;`) store order is `z,y,x` (right-to-left, per
   C++ assignment-operator chaining semantics) while the target stores
   `x,y,z` in declaration order -- fixed by writing three separate
   statements everywhere a vec3 gets a uniform value.

6. **Do not call the base destructor explicitly.** An initial
   `daWmItem_c::~daWmItem_c() { dBaseActor_c::~dBaseActor_c(); }` compiled
   to a *double* `bl __dt__12dBaseActor_cFv` (one from the manual call, one
   auto-generated) plus an unwanted manual secondary-vtable-pointer reset
   at `this+0x60` that the target's dtor never performs. An empty `{}` body
   (letting the compiler auto-generate the destruction chain) is the fix --
   same "do not initialise/destroy what the compiler already handles" rule
   as manta's ctor negative, now confirmed for destructors too.

7. **`mAnmTexPat.mpChildren`, not a separate field, occupies `this+0x1f0`.**
   An earlier layout draft declared a spurious extra `u32 m_1f0;` member
   between `mAnmTexPat` and `mMotion`, based on a naive reading of the
   ctor's zero-stores before the `Probe`-verified `sizeof(anmTexPat_c) ==
   0x2c` was known. Removed once the real size made clear the ctor's
   `stw r31, 0x1f0(r29)` is `anmTexPat_c`'s own last field
   (`mpChildren`), not a class member of `daWmItem_c` -- this was caught
   before it reached the header, but is recorded since it is exactly the
   kind of same-size-different-owner mistake `check_bounds.py`'s docstring
   warns about, just one level down (struct field, not `.data` symbol).

## Tools used beyond the standard set

`tools/relfile.py`'s `Rel` class was loaded directly (not through any
existing wm_units script) to walk `original/d_basesNP.rel`'s own
self-relocation stream (module index == the REL's own index, confirmed 2)
and answer two questions no amount of disassembly reading alone can settle
with certainty: (a) exactly which `.data` addresses are *pointed to* by the
anonymous pointer tables following the profile (their targets never appear
as `.text`-sourced relocations, since they're reached via pointer
arithmetic off the already-relocated profile address, not per-entry
relocations), and (b) exactly which `.data`/`.rodata`/`.ctors` addresses
are referenced *from* this unit's own `.text`/`.ctors` ranges, which is what
let `.data`'s upper bound (`0x45230`, not `0x45270`) be settled with
certainty rather than guessed from the `check_bounds.py` heuristic alone.
Scratch scripts: `dump_relocs.py`, `dump_text_refs.py`, `dump_ctors.py`,
`dump_strings.py`, `dump_prefix.py`, `dump_more.py` (all in this directory).
`probe.cpp`/`probe_build.py` hold the layout-verification compile.

## Round 2: testing the "single named array anchors one register" hypothesis

Coordinator's hypothesis: since separate named `.rodata`/`.data` objects
each get their own `lis`/`addi` pair, consolidating a unit's constants into
ONE object should force every access to share one base register by
construction, the way it already did for dance_pakkun's string pooling.
Tested directly on this unit's three still-open functions. Result: **the
hypothesis is confirmed for multi-use access within one function, not yet
sufficient for single-use access at a non-zero offset, and not sufficient
by itself when the shared object is a different one (profile-relative
tables) that also needs eager hoisting.**

### `.rodata` layout (the one array, target order, values read directly from
`original/d_basesNP.rel` at file offset `0x1c6600 + 0x8988`)

```cpp
static const float sConstTable[12] = {
    -32.0f, 200.0f, 3.2f, 0.65f, 0.0f, 1000.0f,   // lbl_2_rodata_8988, 0x8988-0x89a0
    1.0f, 0.65f, 2.0f, 3.2f, -32.0f, 200.0f,      // lbl_2_rodata_89A0, 0x89a0-0x89b8
};
```
dtk splits this into two symbols; the target's own code (`create()`'s
item-type-5 branch reads `lbl_2_rodata_8988+0x1c`, past the first object's
declared 0x18-byte size, through the same base register as the first 6
floats) proves the real source has one 12-float table. This single
declaration is what let `create()` reach **MATCH** this round (see table
above) -- confirmed working.

### `calcModel()` -- hypothesis CONFIRMED for the rodata axis

Before consolidation (two separate `sRodata8988`/`sRodata89A0`-style named
objects, or `sConstTable` accessed without a local alias): every one of the
4 constant reads (`k[9]`, `k[7]`, `k[10]`, `k[11]`) re-established its own
`lis`/`addi` pair. After declaring `const float *k = sConstTable;` once at
the top and indexing through `k`: **all 4 reads now share ONE `lis
r31,sConstTable@ha; addi r31,r31,sConstTable@l` pair at the top of the
function**, exactly matching the target's single `lis
r31,lbl_2_rodata_8988@ha` anchor reused via `0x24(r31)`/`0x1c(r31)`/
`0x28(r31)`/`0x2c(r31)`. Differing count dropped 68 -> 60 from this change
alone. Register signature: target and draft both save `r30`/`r31` via
individual `stw` (not `_savegpr_NN` -- only 2 non-volatiles needed), and
both now hit that shape. The remaining 60-instruction gap is a *different*
axis entirely -- stack-slot reuse/frame size (target's frame is `0x30`,
draft's is `0x20`; target stages a `mVec3_c`-shaped temporary through
`0x8`/`0xc`/`0x10` that the draft's version doesn't allocate separately,
because the draft's own local `mVec3_c local` has a non-overlapping
lifetime and gets its stack slots coalesced with the earlier scratch) --
not addressed this round.

### `createModel()` -- hypothesis PARTIALLY confirmed, net negative this round

The still-open 116-differing gap here is not about `sConstTable` (already
single-anchored, matching `calcModel()`'s fix) -- it is about the EIGHT
per-item-type tables/strings at `g_profile_WM_ITEM+0x68` through `+0x158`.
Measured directly: the target reaches every one of them through **one
dedicated register, `r30 = &g_profile_WM_ITEM`**, established once near the
top of the function (`lis r30,g_profile_WM_ITEM@ha` immediately after `lis
r31,lbl_2_rodata_8988@ha`, both before the first real work), then reused via
plain `addi rX,r30,offset`. That is the profile-relative-pointer-arithmetic
shape the coordinator's hypothesis predicts -- proven by rewriting the eight
accesses through `WM_ITEM_TABLE(off)`/`WM_ITEM_STR(off)` macros
(`(const u8*)&g_profile_WM_ITEM + off`, in `d_a_wm_item.cpp`, currently
unused but left in place) instead of the separate named globals
(`sItemArcNames` etc.) they replaced. That rewrite DID make the compiler
establish `r30 = &g_profile_WM_ITEM` -- confirming the addressing-mode part
of the hypothesis -- but LAZILY, at the first actual use (after `memset`,
mid-function), not hoisted to the top the way the target's `r30`/`r31` both
are. The target hoists BOTH persistent anchors before `createFrmHeap` is
even called and needs 5 saved registers end to end (`_savegpr_27`); the
macro'd draft only reached 4 non-volatiles, one short of that threshold, and
scored **worse** for it: 116 -> 165 differing, so it was reverted to the
named-global form (worse addressing mode, but no lazy-establishment
penalty) for this round's reported 8/12. **Register-signature finding**:
`_savegpr_27`/`_restgpr_27` (5 registers, r27-r31) appears in the target and
has never appeared in any draft variant tried this round -- that helper-call
threshold, not just the differing count, is the signal to watch. Getting
there likely needs the profile-relative addressing AND something that
forces eager hoisting (an explicit reference near the top of the function,
or possibly reordering which local gets its address taken first) -- neither
alone was enough.

### `cycleAnm()` -- hypothesis produced NO CHANGE (negative, single non-zero-offset access)

`cycleAnm()`'s only `sConstTable` read is index 6 (`sConstTable[6]`, the
first element of the `lbl_2_rodata_89A0` half), used once. The target
folds this into a single `lis r4,lbl_2_rodata_89A0@ha; lfs
f1,lbl_2_rodata_89A0@l(r4)` (2 instructions, offset folded into the
relocation's addend since it is that symbol's own first element). The
draft, whether written as `sConstTable[6]` or as explicit pointer
arithmetic (`*(const float*)((const u8*)sConstTable + 0x18)` -- tried and
reverted, byte-identical result either way), always emits `lis
r5,sConstTable@ha; addi r5,r5,sConstTable@l; lfs f1,0x18(r5)` (3
instructions). **This is a real MWCC folding-heuristic limit, not a
source-modeling gap**: a single access to a non-zero, compile-time-constant
offset within a multi-use symbol does not fold into the `lfs` immediate the
way a same-offset access to that symbol's own dedicated (zero-offset)
identity does, regardless of how the offset is spelled in C++. Every other
difference in `cycleAnm()`'s 14-differing report is downstream of this one
extra instruction (position-shifted matches) or plain symbol-name text
(`lbl_2_data_451B0` vs the local `sCycleAnmNames`) -- confirmed by direct
`difftool.py` diff, not `verify_anon.py` pairing.

### `__sinit` (`fn_2_1678F0`) -- left alone this round per instruction

Still 15 differing, all symbol-name text plus the same `lis+addi+lfs` vs
`lis+lfs` gap as `cycleAnm()` (its 4 float reads are `sConstTable[0..3]`,
the table's own first four elements at offset 0 -- these SHOULD fold
per the "own first element" pattern above, but this function has 4
separate reads at DIFFERENT offsets from one base, i.e. multi-use, which is
exactly the shape that worked for `calcModel()`). Not re-measured after the
`calcModel()` fix or the `.data` reorder (profile now declared before the
table block) -- flagged for next round per the coordinator's "leave it
until after" instruction, but worth checking first since it may already be
closer than last measured.

### Overall

Score unchanged at **8/12** this round (the two structural fixes that did
land -- `.data` declaration reordered to open on the profile, and
`calcModel()`'s single-array anchor -- improved instruction-level
similarity on already-non-matching functions but did not flip any of them
to MATCH). The hypothesis is real and partially validated, but this unit's
remaining gap is now better understood as at least two independent axes
(rodata/table addressing-mode, and stack-frame/register-hoisting
scheduling), not one.

## Round 3: `calcModel`'s second axis, following the kinoballoon lever in reverse

Coordinator's read of the remaining 60-differing gap: the target stages a
`mVec3_c`-shaped temporary the draft doesn't, and the mechanism that
produces an unread stack temp is a by-reference argument -- the "reverse"
of kinoballoon's addend-vs-result finding. Tested directly.

**What the target actually stores into the mystery slot (`0x8`/`0xc`/`0x10`,
read via `difftool.py`, not guessed): the addend `k[7]` (0.65), written
three times, immediately followed by `fmuls f0,f0,f1` computed from the
*register* (not read back from the slot) -- i.e. a genuinely unread stack
write.** `mVec3_c` has no scalar-broadcast constructor and no `operator*=`
taking a `const mVec3_c&` (only `operator*=(f32)`, which needs no by-
reference storage) -- so the by-reference-parameter theory doesn't apply
literally here. What DOES reproduce the exact shape is the plain 3-argument
constructor called with the same value three times:

```cpp
mVec3_c tmp(k[7], k[7], k[7]);
float v = k[9] * tmp.x;
mScale.x = v; mScale.y = v; mScale.z = v;
```

Result: **60 -> 15 differing, and the instruction count now matches exactly
(70 vs 70, frame `0x30` both sides)**. This is the single largest jump of
either round. Every remaining difference is either symbol-name text
(`sConstTable` vs `lbl_2_rodata_8988`) or one specific residual: the
draft's `tmp` (conditional, declared inside the `if (mItemType==5)` block)
and `local` (unconditional, declared after) land on the OPPOSITE stack
slots from the target -- target puts the conditional/first-used one at the
lower address (`0x8-0x10`) and the unconditional/second-used one higher
(`0x14-0x1c`); the draft does the reverse. Tried: moving `tmp`'s
declaration outside the `if` (unconditional, same nesting as `local`) --
made it worse, 15 -> 21, reverted. Tried: giving `local` the same
3-argument-constructor form as `tmp` instead of field-by-field assignment
-- no change, same 15. Not resolved this round; a nesting-depth or
scope-lifetime heuristic in MWCC's stack allocator, not an addressing or
temp-materialization question, is now the sole remaining gap on this
function, and it is genuinely narrow (2 slots swapped, nothing else).

`__sinit` re-tested per the "shares the pool" note: **unchanged, still 15
differing.** It is a separate function with its own independent codegen, so
`calcModel`'s fix does not carry over automatically. Its own shape is a
distinct partial case of the offset-folding limit: it reads `sConstTable[0..3]`
(4 reads, multi-access -- the case that DID consolidate to one register in
`calcModel`), but only 3 of the 4 (`[1]`,`[2]`,`[3]`) share one anchor;
the first (`[0]`, offset 0 -- an object's own first element, the same
condition that let `cycleAnm()`'s target fold to 2 instructions) still gets
its own separate `lis`/`lfs` pair, matching the "offset-0 access folds
specially" rule found in `cycleAnm()`, just showing up as a partial
effect here instead of an all-or-nothing one.

**Score: still 8/12 (no function crossed to MATCH this round), but
`calcModel` moved from 60 to 15 differing with matching instruction count,
easily the strongest position of the three still-open functions.**
`createModel` (116) and `cycleAnm` (14) are unchanged, left alone per
instruction.
