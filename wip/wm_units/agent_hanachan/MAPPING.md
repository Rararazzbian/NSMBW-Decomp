# WM_HANACHAN mapping notes

`.text 0x164430-0x165c70` (0x1840 bytes), `daWmHanachan_c : public dWmDemoActor_c`,
`sizeof 0xf00`. 32 real functions total (confirmed by walking every address in the
range via the MCP `get_target` tool against `d_basesNP`, which resolves real
symbol names from `bin/dtk/wiimj2d_symbols.txt` -- much faster than reasoning from
`verify_anon`'s "vs ~nearest-size-guess" placeholder names, which are NOT real
target identifications, just size-based nearest neighbours for diff purposes).

## Tally this round

Start of round (measured): **13/32**.
End of round (measured): **16/32**. New matches this round: `unkFn164B10`,
`resetTrail`, `resetTargetPositions`.

`FUNCTION ORDER IS WRONG` still fires -- expected per HANDOFF's standing note,
this clears itself as more functions land and must not be "fixed" by manually
reordering already-correct code.

## Full function list (address order), this round's findings

| addr      | bytes | status                          | notes |
|-----------|-------|----------------------------------|-------|
| 164430 | 0x30 | MATCH | classInit |
| 164460 | 0x124| MATCH | ctor |
| 164590 | 0x4  | MATCH | `mVec3_c` trivial ctor (shared with HanachanState_t's) |
| 1645a0 | 0x40 | MATCH | `HanachanState_t` vector-deleting dtor |
| 1645e0 | 0x11c| MATCH | dtor |
| 164700 | 0x64 | MATCH | create |
| 164770 | 0xb0 | 5 differing | execute -- unauthored this round, prior wall: two constants sourced from separate anonymous pool slots instead of one named table |
| 164820 | 0x84 | MATCH | draw |
| 1648b0 | 0x8  | MATCH | doDelete |
| 1648c0 | 0x244| UNWRITTEN | createModel -- placeholder only |
| 164b10 | 0x78 | **MATCH (new)** | `for(i<4) mModels[i].play(); mModel2.play();` -- vtable slot 0x1c on `m3d::mdl_c` is `play()`, confirmed by dumping the real DOL vtable at `__vt__Q23m3d5mdl_c` (0x80329980, size 0x20 = 8 slots) and resolving each slot's address against `bin/dtk/wiimj2d_symbols.txt` |
| 164b90 | 0x17c| UNWRITTEN | resetState -- placeholder |
| 164d10 | 0xfc | UNWRITTEN | calcModel -- placeholder |
| 164e10 | 0x98 | UNWRITTEN | state4WhenNear -- placeholder |
| 164eb0 | 0x1d4| UNWRITTEN | state0 (sStateTable[0]) -- full disasm pulled this round (see below), not yet authored |
| 165090 | 0xc  | MATCH | setState1 |
| 1650a0 | 0x6c | MATCH | state1 |
| 165110 | 0xc  | MATCH | setState2 |
| 165120 | 0x78 | 21 differing | state2 -- prior wall: `cror`-combine operator-spelling sensitivity, characterised, not solved |
| 1651a0 | 0xc  | MATCH | setState3 |
| 1651b0 | 0x404| UNWRITTEN | state3 (sStateTable[3]) -- largest function in the unit, never attempted |
| 1655c0 | 0xb8 | **MATCH (new)** | `resetTargetPositions()`: `mPos=getBasePos(); mUnk460=getPosVariant2(); mUnk46c=getPosVariant3(); mUnk454=getBasePos();` (yes, `getBasePos()` really is called twice) |
| 165680 | 0x74 | **MATCH (new)** | `resetTrail()`: `for(i<200) mTrail[i]=getPosVariant3();` |
| 165700 | 0x68 | 19 differing | `getPosVariant3()` -- see "open wall" below |
| 165770 | 0x68 | 19 differing | `getPosVariant2()` -- same wall |
| 1657e0 | 0x90 | 32 differing | `getBasePos()` -- same wall, root cause |
| 165870 | 0x1c | MATCH | resetPosFromState0 |
| 165890 | 0x110| UNWRITTEN | never declared in the class at all yet; not investigated this round |
| 1659a0 | 0x10c| UNWRITTEN | unkFn1659A0(float,float) -- placeholder, loops over mStates[5] |
| 165ab0 | 0x70 | UNWRITTEN | unkFn165AB0 -- placeholder, loops 5x over a pointer array at `this+0x440` calling a virtual (vtable slot 5) on each -- **not yet reconciled** with the `mUnk440`/`mSmdls` layout; likely a SEPARATE array of 5 pointers somewhere, not investigated |
| 165b20 | 0x128| UNWRITTEN | NOT a `daWmHanachan_c` member -- looks like a static/global-scope object's `__sinit`-style initialiser (`__register_global_object`, writes into `lbl_2_bss_*`/`lbl_2_rodata_*` at fixed addresses, no `this` parameter at all). Needs separate investigation, not part of the class's own 32 in the usual sense but occupies a `.text` slot in range. |
| 165c50 | 0x1c | 7 differing | `HanachanState_t`'s own array-element ctor (mangled `__ct__Q214daWmHanachan_c15HanachanState_tFv`) -- currently auto-generated by the compiler for the `mStates[5]` `__construct_array` call; the "7 differing" is almost certainly just the trivial-ctor codegen not matching some detail, low priority |

## New layout correction this round

The previous round's single-float guess for `+0x454`/`+0x46c` was WRONG in type
(right offset, wrong width). Real layout, confirmed directly from
`resetTargetPositions()`'s own stores:

```
+0x454  mVec3_c mUnk454   (state1/state2 compare only .x -- why the old float guess still matched there)
+0x460  mVec3_c mUnk460
+0x46c  mVec3_c mUnk46c
+0x478  u8 pad[4]
+0x47c  int mState
```

`mPos` (inherited, NOT `mTargetPos`) is confirmed at `+0xac` from `create()`'s own
already-matching `lfs f3, 0xac(r31)`. Do not reach for `mTargetPos` (`+0x168` once
compiled per the shared header) when you see a Vec3 store at `+0xac` -- that
address is `mPos`.

## Header change: `daWmMap_c::GetPos(const char*)` -- APPLIED, real header, not a shadow anymore

Proposed this round from a shadow copy in `shadow_include/game/bases/d_a_wm_map.hpp`;
the lead checked the evidence and applied it directly to the real
`include/game/bases/d_a_wm_map.hpp` (plus a `syms.txt` entry), five binaries
re-verified green. The shadow copy has been DELETED from this unit's
`shadow_include/` -- there is nothing left there. Rebuilt against the real
header afterward: tally unchanged at 16/32, all call sites compile identically.

Evidence that carried it: `bin/dtk/wiimj2d_symbols.txt` line
"GetPos__9daWmMap_cFPCc = .text:0x80100380; // size:0x50" sits immediately
after the already-landed `GetPos__9daWmMap_cFi = .text:0x80100310; // size
0x64", and `getBasePos()`'s target (`fn_2_1657E0`) calls it directly by that
exact mangled name with the by-value-return-via-hidden-pointer convention
already established for the `int` overload, passing a name-string literal
("W502", read directly out of `original/d_basesNP.rel` at the
relocation-resolved `.data` address). Mirrors the already-landed
`GetNodePos(const char*, mVec3_c&)` overload in the same header.

## Function-order gate: VERIFIED as the known partial-authoring artifact, not real

Lead asked me to actually check this rather than wave it off, since `verify_anon.py`
was fixed mid-round. Direct proof, not inference: I disassembled the REAL,
LANDED, currently-matching compiled object
`bin/compiled/d_basesNP/d_basesNP/bases/d_a_wm_kinoko_base.o` (via
`dtk elf disasm`). Its OWN `ACTOR_PROFILE` macro sits at the physical end of
`d_a_wm_kinoko_base.cpp`, exactly like ours -- yet the compiled object's very
FIRST function is `daWmKinokoBase_c_classInit__Fv`, ahead of the constructor and
everything else. So MWCC genuinely relocates the `ACTOR_PROFILE`-generated
`_classInit` to the front of the object regardless of its source position, on a
file we KNOW links and matches today. That is the exact same shape as our
draft's reported violation (classInit's draft-index is high, so everything
after it in target order reads as "defined too late"). Confirmed benign; not
reordering anything.

## Open wall: `getBasePos`/`getPosVariant2`/`getPosVariant3` -- shape now closed, registers still open

Lead traced the mechanism centrally: `nw4r::math::VEC3::operator+`
(`include/lib/nw4r/math/math_types.h:119`) wraps an inline `VEC3Add`
(same file, line 315) that emits exactly this psq_l/ps_add/psq_st shape,
distinct from `PSVECAdd` (real `bl`, confirmed) and from `mVec3_c::operator+`
(scalar, confirmed). Applied it here:

```cpp
static const nw4r::math::VEC3 kOffsetA(0.0f, 0.0f, 0.0f); // + kOffsetB, kOffsetC

mVec3_c daWmHanachan_c::getBasePos() {
    mVec3_c pos = daWmMap_c::m_instance->GetPos("W502");
    const nw4r::math::VEC3 &posRef = pos;   // base-class reference, zero-cost
    return posRef + kOffsetA;               // paired-single VEC3::operator+
}
```

**Result: the INSTRUCTION SHAPE now matches** -- `psq_l`/`ps_add`/`psq_st` appear
exactly where the target has them, plus the trailing 3-scalar copy into the
`mVec3_c` return slot (which is `mVec3_c(const nw4r::math::VEC3&){set(...)}`'s
own shape, byte-plausible). This took `getBasePos` from 32/33 differing down to
29, and `getPosVariant2`/`getPosVariant3` similarly improved. **None closed to
MATCH yet** -- residual is now purely register allocation / instruction
scheduling, not a wrong construct:

- The target computes `r31 = lbl_2_data_44D60` (the TABLE's base symbol, not an
  individually-named `kOffsetA`) BEFORE calling `GetPos`, and indexes into it
  with a raw `+0x4`/`+0x10`/`+0x1c` displacement at the point of use. My draft
  computes each `kOffsetX`'s OWN address (its own `lis`/`addi` pair) AFTER the
  `GetPos` call. This strongly suggests `lbl_2_data_44D60` is genuinely a
  shared/anonymous literal pool spanning MANY functions in this class (the
  previous round already found `lbl_2_data_44D60+0x54/+0x58` used by
  `unkFn1659A0` for its `1.0f`/`1.25f` constants) -- i.e. three SEPARATE named
  `static const` objects is the wrong shape; it should likely be ONE array or
  anonymous literals that MWCC naturally pools together once enough of the
  class's functions that share the table are actually authored. With only 3 of
  the table's consumers written this round, I cannot yet reproduce the full
  pool's shared-base-pointer hoisting.
- GetPos's own destination stack slot also differs (target's GetPos writes
  directly to the slot that becomes the paired-single source with NO
  intermediate copy at `r1+0x8`; mine currently allocates its stack slots in a
  different order -- did not chase further this round).

**Not spending more of this round on it** per lead's instruction. Values and
call graph are correct; only scheduling remains. Next session: try declaring
one shared `static const nw4r::math::VEC3 kTable[]` (or wait until more of the
table's OTHER consumers -- `unkFn1659A0` in particular -- are authored so the
pool has enough real references to reproduce the hoisting), and try swapping
`GetPos`'s call to write directly into the slot later read by `psq_l` instead
of an intermediate.

## Named constants added this round

Three separate `static const Vec` zero-vectors (`kOffsetA/B/C`), declared
together ahead of the `getPosVariant3/2/getBasePos` group so they pool in
ascending `.data` order (`lbl_2_data_44D60+0x4/+0x10/+0x1c`) independent of
which function is defined first -- confirmed by directly reading
`original/d_basesNP.rel`'s `.data` bytes at those offsets (all three are
genuinely `0.0f, 0.0f, 0.0f` in retail, not a placeholder guess). The string
literal `"W502"` used by `getBasePos()` was left as an inline literal (not a
named constant) since nothing else references its pool slot this round.

## How to dump retail REL/DOL bytes directly (used heavily this round)

```
dtk rel info original/d_basesNP.rel     # section file offsets, e.g. .data file off 0x1D0C00
python -c "
with open('original/d_basesNP.rel','rb') as f:
    f.seek(0x1D0C00 + <data-relative-addr>)
    print(f.read(N))
"
```//
For DOL-side (cross-REL/SDK) addresses, `dtk dol info original/wiimj2d.dol` gives
`.text`/`.data` VA + file-offset pairs; same offset-by-VA-delta trick applies to
`original/wiimj2d.dol`. This was decisive for confirming the `GetPos(const
char*)` overload and the `m3d::mdl_c` vtable slot 0x1c identity (`play()`) --
both were unreachable from `verify_anon`'s diff output alone.

## Not investigated this round (parked, not walls)

- `createModel` (1648c0, 0x244), `resetState` (164b90, 0x17c), `calcModel`
  (164d10, 0xfc), `state4WhenNear` (164e10, 0x98), `state0` (164eb0, 0x1d4),
  `state3` (1651b0, 0x404, largest in unit), `unkFn1659A0` (1659a0, 0x10c),
  `unkFn165AB0` (165ab0, 0x70) -- placeholders only, not attempted this round.
  `state0`'s full disassembly WAS pulled this round (see git history / this
  session) and calls `fn_2_165700`/`fn_2_165770` (now named `getPosVariant3`/
  `getPosVariant2`) twice each plus the pattern from `resetTargetPositions` --
  should be tractable next round now that those helpers exist.
- `fn_2_165890` (0x110) -- never declared as a class member at all; not
  looked at this round.
- `fn_2_165ab0`'s `this+0x440` pointer-array-of-5 loop -- doesn't obviously
  reconcile with the current `mSmdls[5]` (ends at 0x440) / `mUnk454` (starts
  0x454) layout. Needs its own investigation, not attempted.
- `fn_2_165b20` (0x128) -- not a class member; looks like a file-static
  object's dynamic initialiser. Not investigated.
